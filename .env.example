# Lodestar — environment variables (Session 1)
# Copy this file to .env.local and fill in your values. NEVER commit .env.local.

# Google Gemini (Google Gen AI SDK: "@google/genai")
GEMINI_API_KEY=
GEMINI_CHAT_MODEL=gemini-3.5-flash
GEMINI_EMBED_MODEL=gemini-embedding-2
EMBED_DIM=1536

# Note: Supabase / database variables are introduced in Session 2.
